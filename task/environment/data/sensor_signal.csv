import numpy as np
import pandas as pd

np.random.seed(42)
fs = 50.0
duration = 4.0
n = int(fs * duration)
t = np.arange(n) / fs

clean = 2.0 * np.sin(2 * np.pi * 3.0 * t) + 0.8 * np.sin(2 * np.pi * 1.0 * t)
noise = 0.6 * np.sin(2 * np.pi * 20.0 * t) + np.random.normal(0, 0.25, n)
raw = clean + noise

anomaly_indices = sorted(np.random.choice(range(20, n - 20), size=5, replace=False).tolist())
for idx in anomaly_indices:
    raw[idx] += np.random.choice([-1, 1]) * np.random.uniform(3.0, 4.5)

pd.DataFrame({"time": t, "value": raw}).to_csv("sensor_signal.csv", index=False)
print("done, anomaly_indices =", anomaly_indices)
